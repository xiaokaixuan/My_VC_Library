UIStyle.manifest   -> Only apply new UI;
UIStyleAd.manifest -> Apply new UI, and execute request adnimistrator permissions.

Usage:
Import .manifest file to resource view, resource type 24, and resource name 1.

